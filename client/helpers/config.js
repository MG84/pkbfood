// In order to configure the accounts UI to use 
// usernames instead of email address
//TODO: check for sending email smtp (reset password)
// At the bottom of the client code
Accounts.ui.config({
  passwordSignupFields: 'USERNAME_ONLY'
});